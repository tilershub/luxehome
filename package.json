{
  "name": "tilershub",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "astro build && wrangler versions upload",
    "postinstall": "astro build"
  },
  "dependencies": {
    "astro": "^5.17.3"
  }
}
