---
---

<nav>
  <a href="#" class="logo">TILERS<span>HUB</span></a>
  <ul>
    <li class="has-dropdown">
      <a href="/#services">Services</a>
      <div class="dropdown">
        <a href="/services/floor-tiling">Floor Tiling</a>
        <a href="/services/staircase-tiling">Staircase Tiling</a>
        <a href="/services/kitchen-pantry">Kitchen Pantry</a>
        <a href="/services/bathroom-renovation">Bathroom Renovation</a>
      </div>
    </li>
    <li><a href="/#materials">Materials</a></li>
    <li><a href="/#bathroom">Bathroom</a></li>
    <li>
      <a
        href="https://wa.me/94774503744?text=Hello%2C%20I%20would%20like%20a%20quote."
        class="nav-cta"
      >Get a Quote</a>
    </li>
  </ul>
</nav>

<script>
  const nav = document.querySelector('nav');
  window.addEventListener('scroll', () => {
    nav.style.padding = window.scrollY > 60 ? '.8rem 4rem' : '1.2rem 4rem';
  });
</script>

<style>
  nav {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 100;
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 1.2rem 4rem;
    background: rgba(245, 240, 232, .93);
    backdrop-filter: blur(14px);
    border-bottom: 1px solid var(--stone);
    transition: padding .3s;
  }

  ul {
    display: flex;
    gap: 2rem;
    list-style: none;
  }

  ul a {
    text-decoration: none;
    font-size: .78rem;
    font-weight: 500;
    letter-spacing: .09em;
    text-transform: uppercase;
    color: var(--warm-gray);
    transition: color .2s;
  }

  ul a:hover {
    color: var(--clay);
  }

  .nav-cta {
    background: var(--charcoal) !important;
    color: var(--cream) !important;
    padding: .5rem 1.2rem;
    border-radius: 2px;
  }

  .nav-cta:hover {
    background: var(--clay) !important;
  }

  /* ── Dropdown ── */

  .has-dropdown {
    position: relative;
  }

  .dropdown {
    position: absolute;
    top: calc(100% + .6rem);
    left: 50%;
    transform: translateX(-50%);
    background: rgba(245, 240, 232, .97);
    backdrop-filter: blur(14px);
    border: 1px solid var(--stone);
    border-radius: 3px;
    min-width: 180px;
    padding: .4rem 0;
    display: none;
    flex-direction: column;
    box-shadow: 0 8px 24px rgba(0, 0, 0, .1);
    z-index: 200;
  }

  .has-dropdown:hover .dropdown,
  .has-dropdown:focus-within .dropdown {
    display: flex;
  }

  .dropdown a {
    display: block;
    padding: .55rem 1.2rem;
    font-size: .75rem !important;
    font-weight: 500 !important;
    letter-spacing: .06em !important;
    color: var(--warm-gray) !important;
    text-transform: none !important;
    white-space: nowrap;
    transition: background .15s, color .15s !important;
  }

  .dropdown a:hover {
    background: var(--sand);
    color: var(--clay) !important;
  }

  @media (max-width: 1100px) {
    nav {
      padding: 1rem 2.5rem;
    }
  }

  @media (max-width: 768px) {
    nav {
      padding: 1rem 1.5rem;
    }

    ul {
      display: none;
    }
  }
</style>
