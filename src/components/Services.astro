---
---

<section id="services">
  <div class="stag reveal">What We Do</div>
  <h2 class="reveal">Services for Every<br />Space & Budget</h2>
  <p class="sdesc reveal">
    Clear pricing, premium materials, and craftsmanship you can see — on every
    job, every time.
  </p>

  <div class="svc-grid reveal">

    <!-- Floor Tiling -->
    <div class="sc">
      <svg class="si" viewBox="0 0 48 48" fill="none">
        <rect x="4"  y="4"  width="18" height="18" rx="1" fill="#c4784a" opacity=".85"/>
        <rect x="26" y="4"  width="18" height="18" rx="1" fill="#c4784a" opacity=".4"/>
        <rect x="4"  y="26" width="18" height="18" rx="1" fill="#c4784a" opacity=".4"/>
        <rect x="26" y="26" width="18" height="18" rx="1" fill="#c4784a" opacity=".85"/>
      </svg>
      <div class="svc-name">Floor Tiling</div>
      <div class="svc-desc">
        Precision installation for tiles up to 4×2. Clean level finish, lasting
        adhesion, tile leveling system used throughout.
      </div>
      <div class="price-row">
        <span class="price-amt">Rs. 350</span>
        <span class="price-unit">/ sq.ft — area</span>
      </div>
      <div class="price-row">
        <span class="price-amt">Rs. 250</span>
        <span class="price-unit">/ lin.ft — skirting</span>
      </div>
      <span class="badge">Up to 4×2 Tile Size</span>
    </div>

    <!-- Staircase Tiling -->
    <div class="sc">
      <svg class="si" viewBox="0 0 48 48" fill="none">
        <rect x="4"  y="37" width="40" height="7" rx="1" fill="#c4784a" opacity=".85"/>
        <rect x="4"  y="28" width="30" height="7" rx="1" fill="#c4784a" opacity=".6"/>
        <rect x="4"  y="19" width="20" height="7" rx="1" fill="#c4784a" opacity=".38"/>
        <rect x="4"  y="10" width="10" height="7" rx="1" fill="#c4784a" opacity=".2"/>
        <rect x="36" y="6"  width="8"  height="38" rx="1" fill="#c4784a" opacity=".9"/>
      </svg>
      <div class="svc-name">Staircase Tiling</div>
      <div class="svc-desc">
        Double nosing with router edge finish on every step. Side elevation
        tiling and skirting included for a complete high-end result.
      </div>
      <div class="price-row">
        <span class="price-amt">Rs. 8,500</span>
        <span class="price-unit">/ step — complete</span>
      </div>
      <span class="badge">Nosing · Router Edge · Skirting</span>
    </div>

    <!-- Kitchen Pantry -->
    <div class="sc">
      <svg class="si" viewBox="0 0 48 48" fill="none">
        <rect x="4"  y="22" width="40" height="4"  rx="1" fill="#c4784a" opacity=".9"/>
        <rect x="4"  y="28" width="40" height="16" rx="1" fill="#c4784a" opacity=".2"/>
        <rect x="8"  y="8"  width="4"  height="12" rx="1" fill="#c4784a" opacity=".55"/>
        <rect x="16" y="8"  width="4"  height="12" rx="1" fill="#c4784a" opacity=".55"/>
        <rect x="24" y="8"  width="4"  height="12" rx="1" fill="#c4784a" opacity=".55"/>
        <rect x="32" y="8"  width="4"  height="12" rx="1" fill="#c4784a" opacity=".55"/>
      </svg>
      <div class="svc-name">Kitchen Pantry</div>
      <div class="svc-desc">
        Countertop and backsplash in one linear-foot rate — perfectly aligned,
        waterproof joints throughout.
      </div>
      <div class="price-row">
        <span class="price-amt">Rs. 5,000</span>
        <span class="price-unit">/ lin.ft</span>
      </div>
      <span class="badge">Top & Backsplash</span>
    </div>

    <!-- Bathroom Renovation (wide card) -->
    <div class="sc wide">
      <svg class="si" viewBox="0 0 48 48" fill="none">
        <rect x="4"  y="4"  width="40" height="40" rx="2" stroke="#c4784a" stroke-width="2.5" fill="none" opacity=".6"/>
        <path d="M13 28 C13 21 35 21 35 28" stroke="#c4784a" stroke-width="2" fill="none" opacity=".5"/>
        <circle cx="18" cy="17" r="3" fill="#c4784a" opacity=".45"/>
        <rect x="20" y="33" width="8" height="9" rx="1" fill="#c4784a" opacity=".38"/>
      </svg>
      <div class="svc-name">Bathroom Construction & Renovation</div>
      <div class="svc-desc">
        Full-scope renovation — demolition to final fittings. All-inclusive
        except tiles, bathware, and tapware. See full scope below ↓
      </div>
      <div class="price-row">
        <span class="price-amt">Rs. 15,000+</span>
        <span class="price-unit">/ sq.ft — all inclusive</span>
      </div>
      <span class="badge">Demo · Mason · Plumbing · Waterproofing · Electrical · Tiling · Ceiling · Fixtures</span>
    </div>

  </div>
</section>

<style>
  section {
    background: var(--charcoal);
    padding: 5.5rem 4rem;
    position: relative;
    overflow: hidden;
  }

  /* Subtle grid overlay */
  section::before {
    content: '';
    position: absolute;
    inset: 0;
    background:
      repeating-linear-gradient(
        0deg,
        transparent,
        transparent 60px,
        rgba(255, 255, 255, .01) 60px,
        rgba(255, 255, 255, .01) 61px
      ),
      repeating-linear-gradient(
        90deg,
        transparent,
        transparent 60px,
        rgba(255, 255, 255, .01) 60px,
        rgba(255, 255, 255, .01) 61px
      );
  }

  /* Override shared text colours for dark background */
  h2 {
    color: var(--cream);
  }

  .sdesc {
    color: rgba(245, 240, 232, .45);
  }

  /* ── Service grid ── */

  .svc-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.5px;
    background: rgba(255, 255, 255, .07);
    border: 1.5px solid rgba(255, 255, 255, .07);
    position: relative;
    z-index: 1;
  }

  .sc {
    background: #211c18;
    padding: 2.2rem;
    position: relative;
    overflow: hidden;
    transition: background .3s;
  }

  .sc:hover {
    background: #28211a;
  }

  .sc::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    height: 2px;
    background: var(--clay);
    transform: scaleX(0);
    transform-origin: left;
    transition: transform .38s;
  }

  .sc:hover::after {
    transform: scaleX(1);
  }

  .sc.wide {
    grid-column: span 2;
  }

  .si {
    width: 2.6rem;
    height: 2.6rem;
    margin-bottom: 1.2rem;
  }

  .svc-name {
    font-family: 'Playfair Display', serif;
    font-size: 1.15rem;
    font-weight: 700;
    color: var(--cream);
    margin-bottom: .4rem;
    line-height: 1.2;
  }

  .svc-desc {
    font-size: .8rem;
    font-weight: 300;
    color: rgba(245, 240, 232, .4);
    line-height: 1.7;
    margin-bottom: 1.2rem;
  }

  .price-row {
    display: flex;
    align-items: baseline;
    gap: .4rem;
    margin-top: .32rem;
  }

  .price-amt {
    font-family: 'Playfair Display', serif;
    font-size: 1.25rem;
    font-weight: 700;
    color: var(--clay);
  }

  .price-unit {
    font-size: .68rem;
    color: rgba(245, 240, 232, .3);
  }

  .badge {
    display: inline-block;
    background: rgba(196, 120, 74, .1);
    border: 1px solid rgba(196, 120, 74, .24);
    color: var(--clay);
    font-size: .65rem;
    font-weight: 500;
    letter-spacing: .05em;
    padding: .18rem .6rem;
    border-radius: 20px;
    margin-top: .6rem;
  }

  /* ── Responsive ── */

  @media (max-width: 1100px) {
    section {
      padding-left: 2.5rem;
      padding-right: 2.5rem;
    }

    .svc-grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .sc.wide {
      grid-column: span 2;
    }
  }

  @media (max-width: 768px) {
    section {
      padding: 4rem 1.5rem;
    }

    .svc-grid {
      grid-template-columns: 1fr;
    }

    .sc.wide {
      grid-column: span 1;
    }
  }
</style>
